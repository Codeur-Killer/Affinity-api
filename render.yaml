services:
  - type: web
    name: affinity-api
    env: node
    region: Frankfurt
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production